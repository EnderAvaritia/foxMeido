[project]
name = "foxMeido"
version = "0.1.0"
description = "foxMeido"
readme = "README.md"
requires-python = ">=3.9, <4.0"

[tool.nonebot]
adapters = [
    { name = "QQ", module_name = "nonebot.adapters.qq" }
]
plugins = []
plugin_dirs = ["plugins"]
builtin_plugins = ["echo", "single_session"]
